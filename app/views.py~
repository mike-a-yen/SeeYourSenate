from app import app
import flask
from datetime import datetime
from app.cluster import plot_2d, plot_3d

@app.route('/')
@app.route('/index')
def index():
    cluster_plot = plot_2d()
    print(cluster_plot)
    return flask.render_template('index.html',
                                 cluster_plot=cluster_plot)
